-- =============================================================
-- Contact reasons (lookup table seed)
-- =============================================================

insert into public.contact_reasons (label, "order") values
    ('Employer Inquiry', 1),
    ('Contracting',      2),
    ('Article Question', 3);
